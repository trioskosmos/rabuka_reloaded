======================================================================
PARSER FIXES - COMPLETE IMPLEMENTATION
======================================================================

PHASE 1 (10 fixes) + PHASE 2 (6 fixes) = 16 fixes total

======================================================================
PHASE 1 - Merged 2025-01-22
======================================================================

Fix 1: propagate_count_source for location_condition/group_condition
  File: parser.py - _walk() in _normalize_effect_tree
  Lines: ~6739-6746
  Problem: "それらの中に"/"これにより" branch only handled
    card_count_condition, skipped location_condition and group_condition
  Fix: Extended the branch to also convert location_condition and
    group_condition to card_count_condition with source:"preceding_moved"
  JSON entries corrected: 4

Fix 2: recently_moved override too aggressive
  File: parser.py - _try_conditional() and _try_baton_touch_effect()
  Lines: ~5394-5400, ~5502-5506
  Problem: Overrode source from "discard" to "recently_moved" whenever
    baton_touch_trigger was true, even for generic discard searches
  Fix: Only override when action text contains "このバトンタッチで"
    (by this baton touch)
  JSON entries corrected: 2

Fix 3: Wrong source for yell-revealed cards
  File: parser.py - _fill_defaults()
  Lines: ~2195-2205
  Problem: "それらのカード" after yell reveal got source:"hand" fallback
  Fix: Detect "それらのカード" in text and set source:"revealed_cards"
  JSON entries corrected: 1 (ダイスキだったらダイジョウブ!)

Fix 4: Wrong condition location for yell-revealed cards
  File: parser.py - _try_distinct()
  Lines: ~628-634
  Problem: _try_distinct matched before _try_card_count and set
    location:"stage" default, missing "エールにより公開された" → revealed_cards
  Fix: Added "エールにより公開された"/"これにより公開された" override
  JSON entries corrected: 2

Fix 5: exclude_group_names overwritten by group_names
  File: parser.py - _fill_defaults()
  Lines: ~2715-2718
  Problem: group_names extraction at line 2716 overwrote
    exclude_group_names set at line 2143, re-adding excluded groups
  Fix: Re-apply exclude_group_names filtering after group_names extraction
  JSON entries corrected: 1

Fix 6: heart_selection for set-all-to-color patterns
  File: parser.py - dispatch table
  Lines: ~3238-3252
  Problem: "ハートを...にする" pattern matched both choice and set patterns
  Fix: Exclude patterns with "すべて" + "{{heart_" (set to specific color)
  Added new set_heart_type dispatch rule for "ハートをすべてheartXXにする"
  JSON entries corrected: 1

Fix 7: Missing activation_condition_parsed for "いる場合のみ"
  File: parser.py - _merge_parenthetical()
  Lines: ~6086-6094
  Problem: parse_condition returned "custom" for "エリアにいる場合のみ"
    patterns, so activation_condition_parsed was not set (only position was)
  Fix: Build condition directly for "エリアにいる場合" patterns
  JSON entries corrected: 3

Fix 8: Missing or_card_types in select_cards
  File: parser.py - _build_reveal_add_discard(), _build_look_select_actions()
  Lines: ~4553-4554 (added helper), ~4617, ~4644 (added calls)
  Problem: "メンバーカードか...ライブカード" patterns in select_cards
    only recorded card_type:"member_card", missing or_card_types
  Fix: Added _add_or_card_types_if_needed() helper, called from both builders
  JSON entries corrected: 3

Fix 9a: Dynamic count reference prefix trimming
  File: parser_utils.py - extract_dynamic_count()
  Lines: ~165-168
  Problem: Reference included action description prefix ("自分のデッキの上から、")
  Fix: Added _trim_reference_prefix() to strip known noise prefixes
  JSON entries corrected: 1

Fix 9b: Missing dynamic_count for energy under member + 1
  File: parser_utils.py - extract_dynamic_count()
  Lines: ~153-163
  Problem: "エネルギーカードの枚数にNを足した枚数" pattern not handled
  Fix: Added new pattern detection for "下にあるエネルギーカードの枚数にNを足した枚数"
  File: parser.py - _fill_defaults()
  Lines: ~2680-2682
  Problem: energy_count remained set even when dynamic_count was extracted
  Fix: Remove energy_count when dynamic_count is set for place_energy_under_member
  JSON entries corrected: 1 (銓 嵐珠)

Fix 10a: group_reference in cost context
  File: parser.py - _extract_basic_cost_fields()
  Lines: ~3568
  Problem: "同じグループ名" in cost text didn't set group_reference
  Fix: Added check for "同じグループ名" → group_reference:"same_group_name"
  JSON entries corrected: 1

Fix 10b: group_reference in effect context for "異なるグループ名"
  File: parser.py - _walk() in _normalize_effect_tree()
  Lines: ~6903-6904
  Problem: "異なるグループ名" pattern not matched (only "グループ名が異なる")
  Fix: Added "異なるグループ名" to detection
  JSON entries corrected: 1

======================================================================
PHASE 2 - Additional Fixes
======================================================================

Fix 11 (was 9c): Score-based energy cost with dynamic_count
  File: parser.py - _try_conditional_sequential()
  Lines: ~5162-5192 (added splitting logic)
  Problem: The ability "selectライブカード + pay energy equal to its score"
    was parsed as only [select, move_cards], missing the pay_energy step.
    Two barriers: "選び" guard on pay_energy dispatch, and count short-circuit
    before extract_dynamic_count.
  Fix: Added splitting logic in _try_conditional_sequential that detects
    "選び" + "{{icon_energy.png|E}}" + "支払う" in the same segment,
    splits on "、" to isolate the energy payment clause, then parses it
    as a separate pay_energy action with dynamic_count from extract_dynamic_count.
  JSON entries corrected: 1 (桜坂しずく PL!N-bp5-003-R)

Fix 12: Missing source for "手札にある" pattern
  File: parser_utils.py - SOURCE_PATTERNS
  Lines: ~378 (added entry)
  Problem: "手札にある" (in hand) was not recognized as a source pattern,
    so move_cards effects using this phrasing had no source field
  Fix: Added ("手札にある", "hand") to SOURCE_PATTERNS
  JSON entries corrected: 1

Fix 13: Normalize "revealed_card" (singular) to "revealed_cards" (plural)
  File: parser_utils.py - SOURCE_PATTERNS (changed source value)
  File: parser.py - _fill_defaults() (normalization), checks at lines 2321, 2613
  Problem: Source pattern produced "revealed_card" (singular) while convention
    uses "revealed_cards" (plural), risking deserialization mismatch
  Fix: Changed source pattern output, added normalization pass in _fill_defaults,
    updated all checks to accept both forms
  JSON entries corrected: 1

Fix 14: Missing destination in move_cards
  File: parser_utils.py - SOURCE_PATTERNS
  Problem: Text "自分の控え室にあるμ'sのライブカードを1枚置いてもよい"
    has source but no destination. Context implies success_live_card_zone.
  Fix: Added source pattern handling; destination inference from context
    is handled by engine (conditional_alternative propagates destination)
  JSON entries corrected: 1

Fix 15: count=0 placeholder for dynamic draw
  File: parser.py - _fill_defaults()
  Lines: ~2368-2373
  Problem: "これにより置いた枚数分カードを引く" set count=0 as placeholder
    instead of using a proper dynamic_count
  Fix: Replaced count=0 with dynamic_count of type "drawn_cards" with
    reference "previous_draw"
  JSON entries corrected: 1

Fix 16: Condition without text field
  File: parser.py - _walk() in _normalize_effect_tree()
  Lines: ~6623-6626
  Problem: Some condition dicts in the effect tree lacked the human-readable
    "text" field, which could affect debugging and downstream tooling
  Fix: In _walk(), ensure every dict with "type" or "action" also has "text",
    propagated from context text or full_text
  JSON entries corrected: 11

======================================================================
TEST COVERAGE
======================================================================

Python test files with hard assertions:
  cards/ability_extraction/common_phrases/test_fixes.py (Phase 1)
  cards/ability_extraction/common_phrases/test_phase2_fixes.py (Phase 2)

Each test parses raw Japanese card text and asserts specific JSON fields
match expected values, verifying the parser output matches card behavior.

======================================================================
FILES MODIFIED
======================================================================

  cards/ability_extraction/parser.py
  cards/ability_extraction/parser_utils.py
  cards/ability_extraction/common_phrases/test_phase2_fixes.py
  cards/abilities.json (regenerated)
  engine/src/ability/effects/misc.rs (Rust compilation fix)

======================================================================
