diff --git a/platforms/gba/src/display.rs b/platforms/gba/src/display.rs
index 56bedcf2..b3724d67 100644
--- a/platforms/gba/src/display.rs
+++ b/platforms/gba/src/display.rs
@@ -510,7 +510,7 @@ impl<'a> Display<'a> {
                     WAITED_FRONTS,
                     BACK_FRONT,
                     flipped,
-                    Priority::P1,
+                    Priority::P0,
                 ) {
                     self.active_sprites.push(sprite);
                 }
@@ -528,7 +528,7 @@ impl<'a> Display<'a> {
                     WAITED_FRONTS,
                     BACK_FRONT,
                     flipped,
-                    Priority::P1,
+                    Priority::P0,
                 ) {
                     self.active_sprites.push(sprite);
                 }
@@ -546,7 +546,7 @@ impl<'a> Display<'a> {
                     WAITED_FRONTS,
                     BACK_FRONT,
                     flipped,
-                    Priority::P1,
+                    Priority::P0,
                 ) {
                     self.active_sprites.push(sprite);
                 }
@@ -565,7 +565,7 @@ impl<'a> Display<'a> {
                 WAITED_FRONTS,
                 BACK_FRONT,
                 false,
-                Priority::P1,
+                Priority::P0,
             ) {
                 self.active_sprites.push(sprite);
             }
@@ -679,7 +679,8 @@ impl<'a> Display<'a> {
         // Create detail card sprite if art provided (96x144 = 64x64 + 32x64, use 64x64 for now)
         if let Some(art) = art {
             // Use S64x64 for the portrait (will clip to 96x144)
-            let sprites = self.sprite_cache.get_or_create("detail", "detail", art.tiles, Size::S64x64);
+            let card_no = &art.card_no;
+            let sprites = self.sprite_cache.get_or_create(card_no, "detail", art.tiles, Size::S64x64);
             let mut obj = Object::new(sprites[0].clone());
             obj.set_pos((0, DETAIL_Y0 * 8));
             obj.set_priority(Priority::P0);
@@ -829,7 +830,7 @@ impl<'a> Display<'a> {
                 let sprites = self.sprite_cache.get_or_create(&q.card_no, size_tag, front.tiles, size);
                 let mut obj = Object::new(sprites[0].clone());
                 obj.set_pos((q.x * 8, q.y * 8));
-                obj.set_priority(Priority::P1);
+                obj.set_priority(Priority::P0);
                 if q.dimmed {
                     // TODO: implement dimming via palette swap or alpha
                 }
@@ -839,7 +840,7 @@ impl<'a> Display<'a> {
                 }
                 self.active_sprites.push(CardSprite {
                     objects: alloc::vec![obj],
-                    priority: Priority::P1,
+                    priority: Priority::P0,
                 });
             } else {
                 Self::blit_line(&mut self.text_bg, &font_ts, &icon_ts, e, &q.card_no, q.x, q.y);
