[package]
name = "rabuka_ds"
version = "0.1.0"
edition = "2021"

[dependencies]
rabuka_engine = { path = "../../engine", default-features = false, features = ["ds"] }
log = { version = "0.4", default-features = false }
