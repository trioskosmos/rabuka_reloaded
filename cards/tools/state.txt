=== CURRENT STATE ===

Entries with differences: 220 (down from 375)

=== REMAINING ISSUES ===

1. _add_text_to_sub_actions overwrites sub-action text with parent_text
   - actions[] items get full combined text instead of action-specific text
   - Fix: use sub.get('text', parent_text) not parent_text

2. check_self: True not set for discard-pile activation conditions
   - _merge_parenthetical() only checks for センター/サイド/エリアにいる場合
   - Need: also check 控え室にある場合

3. exclude_self leaks from condition to sub-actions (7 entries)
   - parse_action() adds exclude_self for このメンバー以外 patterns
   - Need: cleanup in handler return

4. alternative_condition missing (4 entries)
   - _try_conditional_alternative handler returns wrong structure

5. Condition fields stripped (operator, count, cost_total)
   - parse_condition() produces them but strips somewhere

6. heart_colors not propagated to actions[] items
   - process_abilities() adds at effect level but not sub-actions

=== MODIFIED FILES ===

parser.py changes: 
  - _add_text_to_sub_actions() added (line ~5755)
  - extract_optional() at handler return (line ~5857)
  - extract_optional() in fallback path (line ~5970)
  - check_self: True in parse_condition handler loop (line ~1772)
  - extract_source() handles ステージにいる (line ~140)
  - source extraction for change_state/move_cards in handler return (line ~5852)
  - exclude_self cleanup in handler and fallback return (line ~5847, ~5865)
  - Combined heart+blade handler added to _R (line ~2857)
  - _try_timing_condition_gain handler added to _EFFECT_HANDLERS (line ~5589)

extract_card_abilities.py: scaffolding-only (no parsing calls)

=== VALIDATION ===
python cards/tools/validate_diff.py - counts remaining diffs
git diff --no-index ... - shows actual diff

=== FILES ===
cards/ability_extraction/parser.py - main parser (6858 lines)
cards/ability_extraction/parser_utils.py - parser utilities
cards/ability_extraction/extract_card_abilities.py - extract (scaffolding)
cards/tools/validate_diff.py - diff validation
C:/Users/trios/Downloads/rabuka_reloaded-master (2)/rabuka_reloaded-master/cards/abilities.json - working file
