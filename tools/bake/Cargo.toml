[package]
name = "bake_cards"
version = "0.1.0"
edition = "2021"
description = "Unified bake tool: produces baked card data for PSP (per-deck JSON), DS (RBCD binary), and 3DS (MessagePack)."

[dependencies]
serde_json = "1"
rmp-serde = "=1.3.1"
rabuka_engine = { path = "../../engine" }
