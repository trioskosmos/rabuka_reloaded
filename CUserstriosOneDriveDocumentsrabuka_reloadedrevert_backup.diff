diff --git a/cards/abilities.json b/cards/abilities.json
index 557f4ab..a37435e 100644
--- a/cards/abilities.json
+++ b/cards/abilities.json
@@ -1,6 +1,6 @@
 {
   "schema": "extracted_abilities.v1",
-  "generated_at": "2026-06-19T23:24:14.252227",
+  "generated_at": "2026-06-20T00:18:47.029482",
   "generated_by": "tools/ability_extraction/extract_card_abilities.py",
   "source_file": "C:\\Users\\trios\\OneDrive\\Documents\\rabuka_reloaded\\cards\\cards.json",
   "statistics": {
@@ -28445,7 +28445,7 @@
         "duration": "live_end",
         "actions": [
           {
-            "text": "相手は余剰ハートをすべて失う。",
+            "text": "相手は余剰ハートをすべて失う",
             "action": "gain_resource",
             "sign": "negative",
             "resource": "surplus_heart",
@@ -28460,7 +28460,16 @@
             "value": 1,
             "operation": "add",
             "self_target": true,
-            "duration": "live_end"
+            "duration": "live_end",
+            "condition": {
+              "text": "これにより相手が余剰ハートを2つ以上失っている場合",
+              "resource_type": "surplus_heart",
+              "count": 2,
+              "target": "opponent",
+              "delta": true,
+              "operator": ">=",
+              "type": "comparison_condition"
+            }
           }
         ]
       }
diff --git a/cards/ability_extraction/parser.py b/cards/ability_extraction/parser.py
index 0a3e5ee..acfc561 100644
--- a/cards/ability_extraction/parser.py
+++ b/cards/ability_extraction/parser.py
@@ -1603,6 +1603,10 @@ def _extract_generic_fields(condition, text):
         sc = extract_count(text)
         if sc:
             condition["count"] = sc
+        if "相手" in text:
+            condition["target"] = "opponent"
+        if "失っている" in text and "これにより" in text:
+            condition["delta"] = True
 
     # Card type, count, operator
     ct = extract_card_type(text)
@@ -3296,15 +3300,21 @@ def parse_action(text: str) -> Dict[str, Any]:
     R(
         lambda t: "を失う" in t or "をすべて失う" in t,
         "gain_resource",
-        lambda t, a: a.update(
-            {
-                "sign": "negative",
-                "resource": "surplus_heart"
-                if "余剰ハート" in t or "余分ハート" in t
-                else "heart",
-                "all": "すべて" in t or None,
-            }
-        ),
+        lambda t, a: (
+            a.update(
+                {
+                    "sign": "negative",
+                    "resource": "surplus_heart"
+                    if "余剰ハート" in t or "余分ハート" in t
+                    else "heart",
+                    "all": "すべて" in t or None,
+                }
+            ),
+            a.update({"target": "opponent", "action_by": "opponent"})
+            if "相手" in t
+            else None,
+        )
+        and None,
     )
     R(
         lambda t: "もう一度エール" in t or "もう1度エール" in t,
@@ -4665,6 +4675,10 @@ def _try_opponent_action(text):
     """
     if not text.startswith("相手は"):
         return None
+    # Defer to _try_kore_niyori_result for "これにより...場合" patterns
+    # that have a conditional followup (e.g. surplus heart loss).
+    if "これにより" in text and "場合" in text:
+        return None
     om = re.match(r"相手は[、]?(.+?)(?:。|$)", text)
     if not om:
         return None
@@ -6260,8 +6274,15 @@ def _try_lose_resource(text):
     # Detect what's being lost
     if "ブレード" in text:
         result["resource"] = "blade"
+    elif "余剰ハート" in text:
+        result["resource"] = "surplus_heart"
+        result["all"] = True
     elif "ハート" in text:
         result["resource"] = "heart"
+    # Set target from text prefix (e.g. "相手は" → opponent)
+    if "相手" in text:
+        result["target"] = "opponent"
+        result["action_by"] = "opponent"
     # Count icons in the lost resource
     blade_count = len(re.findall(r"\{\{icon_blade\.png\|ブレード\}\}", text))
     if blade_count > 0:
@@ -8360,6 +8381,9 @@ def process_abilities(data: Dict[str, Any]) -> Dict[str, Any]:
                 actions = [pe]
                 fa = eff.get("followup_action")
                 if isinstance(fa, dict):
+                    rc = eff.get("result_condition")
+                    if isinstance(rc, dict):
+                        fa["condition"] = rc
                     actions.append(fa)
                 if actions:
                     eff["action"] = "sequential"
diff --git a/engine/src/ability/condition/card.rs b/engine/src/ability/condition/card.rs
index 0f8440b..94a8956 100644
--- a/engine/src/ability/condition/card.rs
+++ b/engine/src/ability/condition/card.rs
@@ -2637,6 +2637,13 @@ impl<'a> ConditionContext<'a> {
             }
         }
         if resource_type == Some("surplus_heart") {
+            // delta=true means the condition should check the count that was LOST
+            // by the preceding action, not the current absolute surplus.
+            if condition.delta == Some(true) {
+                let delta_count = self.game_state.mods.last_surplus_loss_count;
+                eprintln!("[DELTA_SURPLUS] last_surplus_loss_count={}", delta_count);
+                return delta_count;
+            }
             // After live clearance the computed surplus count is stored on GameState.
             // Prefer the stored snapshot value over a runtime recalculation
             // (includes yell ALL hearts that can fill any color gap).
@@ -2922,6 +2929,16 @@ impl<'a> ConditionContext<'a> {
                     .sum()
             }
             "surplus_heart" => {
+                // When delta=true, read the count saved before the preceding step
+                // zeroed the surplus, instead of computing from current state.
+                if condition.delta == Some(true) {
+                    let count = self.game_state.mods.last_surplus_loss_count;
+                    return compare_counts(
+                        condition.operator.as_deref(),
+                        count,
+                        condition.count.unwrap_or(1),
+                    );
+                }
                 // Count surplus hearts for the target player
                 let heart_total: u32 = player
                     .stage
diff --git a/engine/src/ability/effects/misc.rs b/engine/src/ability/effects/misc.rs
index 4b8188e..be0b85b 100644
--- a/engine/src/ability/effects/misc.rs
+++ b/engine/src/ability/effects/misc.rs
@@ -359,6 +359,11 @@ impl AbilityResolver {
 
         if resource == "surplus_heart" {
             if sign == Some("negative") && is_all {
+                let old = match target.as_str() {
+                    "opponent" => gs.opponent_live_surplus_count,
+                    _ => gs.self_live_surplus_count,
+                };
+                gs.mods.last_surplus_loss_count = old;
                 if target == "opponent" {
                     gs.opponent_live_surplus_count = 0;
                 } else {
diff --git a/engine/src/ability/effects/mod.rs b/engine/src/ability/effects/mod.rs
index 24f1464..dbd2861 100644
--- a/engine/src/ability/effects/mod.rs
+++ b/engine/src/ability/effects/mod.rs
@@ -23,10 +23,16 @@ impl AbilityResolver {
         let mut dbg = AbDebug::new();
         dbg.effect(effect);
         log::debug!(
-            "DEBUG: execute_effect - action: {}, source: {}, destination: {}",
+            "DEBUG: execute_effect - action: {}, source: {}, destination: {}, has_condition: {}",
             effect.action,
             effect.source_or("none"),
-            effect.destination.as_deref().unwrap_or("none")
+            effect.destination.as_deref().unwrap_or("none"),
+            effect.condition.is_some()
+        );
+        eprintln!(
+            "[EXEC_EFFECT] action={} has_condition={}",
+            effect.action,
+            effect.condition.is_some()
         );
         if !self.can_activate_effect(gs, effect) {
             log::debug!("DEBUG: cannot activate effect");
diff --git a/engine/src/core/game_modifiers.rs b/engine/src/core/game_modifiers.rs
index 3c60f2f..d04b708 100644
--- a/engine/src/core/game_modifiers.rs
+++ b/engine/src/core/game_modifiers.rs
@@ -54,6 +54,9 @@ pub struct GameModifiers {
     /// Per-card delayed "cannot activate" flags. Card_id → remaining turns of
     /// activation block. Decremented each Active phase; member stays wait while >0.
     pub delayed_cannot_active: HashMap<i16, u32>,
+    /// The surplus heart count just before it was zeroed by gain_resource(surplus_heart).
+    /// Used by `delta: true` conditions on subsequent steps.
+    pub last_surplus_loss_count: u32,
 }
 
 impl Default for GameModifiers {
@@ -83,6 +86,7 @@ impl GameModifiers {
             last_cost_discard_count: 0,
             last_cost_energy_count: 0,
             delayed_cannot_active: HashMap::new(),
+            last_surplus_loss_count: 0,
         }
     }
 
